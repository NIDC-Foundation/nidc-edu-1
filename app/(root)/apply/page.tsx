import ApplyClient from "./_components/apply-client";

export const metadata = {
  title: "Apply | NIDC Foundation",
  description: "Apply for a fully funded scholarship through NIDC Foundation.",
};

const ApplyPage = () => {
  return <ApplyClient />;
};

export default ApplyPage;
