import CourseClient from "./_components/course-client";

export const metadata = {
  title: "Programmes | NIDC Foundation",
  description:
    "Explore scholarship-supported degree programmes across our partner universities.",
};

const CoursePage = () => {
  return <CourseClient />;
};

export default CoursePage;
