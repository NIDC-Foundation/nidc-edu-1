import TransparencyClient from "./_components/transparency-client";

export const metadata = {
  title: "Transparency | NIDC Foundation",
  description:
    "Track donations, funded students, and live scholarship allocations at NIDC Foundation.",
};

const TransparencyPage = () => {
  return <TransparencyClient />;
};

export default TransparencyPage;
