import ContactClient from "./_components/contact-client";

export const metadata = {
  title: "Contact | NIDC Foundation",
  description:
    "Get in touch with NIDC Foundation for scholarships, partnerships, donations, or press enquiries.",
};

const ContactPage = () => {
  return <ContactClient />;
};

export default ContactPage;
